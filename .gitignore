# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*


CLAUDE.md

dev-dist
# Node
node_modules
dist
dist-ssr
.vite
*.local

# Env / secrets
.env
.env.*
server/.env
server/.env.*

# Electron build
release
win-unpacked
dist-electron
*.exe
*.blockmap

# Android build
android/.gradle
android/build
android/app/build
android/local.properties
android/.idea
android/*.iml

# Signing keys (ВАЖНО)
*.jks
*.keystore
android/key.properties

# IDE
.vscode/*
!.vscode/extensions.json
.idea

# OS
.DS_Store
Thumbs.db

# Misc
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Локальный дрейф зависимостей (см. "Known environment gap" в CLAUDE.md) — эти файлы уже
# закоммичены в истории, поэтому .gitignore сам по себе НЕ спрячет их изменения от `git add -A`.
# Дополнительно применён `git update-index --skip-worktree` на эти 4 файла — см. пояснение в чате.
package.json
package-lock.json
server/package.json
server/package-lock.json